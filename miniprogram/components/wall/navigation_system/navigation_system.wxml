<!--components/wall/navigation_system/navigation_system.wxml-->

<scroll-view class="icons-container"
  scroll-x scroll-with-animation
  scroll-left="scrollLeft">

  <view wx:for="{{ tabbarItem }}" wx:key="{{ index }}"
    class="icon-group {{ currentTab == index ? 'icon-selected' : '' }}"
    data-current="{{ index }}" catchtap="swichNavigator">
    <view class="icon-wrapper">
      <text class="iconfont {{ item.iconfont }}"></text>
      <text class="icon-text">{{ item.name }}</text>
    </view>
  </view>

</scroll-view>

<swiper current="{{ currentTab }}" bindchange="switchTab"
  style="height:{{winHeight}}px">
  <!-- <swiper-item wx:for="{{ tabbarItem }}" wx:key="{{ index }}">
    <scroll-view scroll-y>
      <text>{{ item.name }}</text>
    </scroll-view>
  </swiper-item> -->
  <swiper-item>
    <scroll-view class="card-group" style="transform: translateY(-70rpx);" scroll-y>
      
      <card />
      <card />
      <card />
      <card />
      <card />
    </scroll-view>
  </swiper-item>

  <swiper-item>222</swiper-item>
  <swiper-item>333</swiper-item>
  <swiper-item>444</swiper-item>
</swiper>